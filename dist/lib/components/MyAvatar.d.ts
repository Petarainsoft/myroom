import { default as React } from 'react';
import { MyAvatarProps, MyAvatarRef } from '../types';
/**
 * MyAvatar - Simplified 3D avatar component
 *
 * A focused React component that provides 3D avatar visualization
 * and customization capabilities using Babylon.js.
 * Supports both simple mode (3D scene only) and full mode (with UI overlay).
 */
export declare const MyAvatar: React.ForwardRefExoticComponent<MyAvatarProps & React.RefAttributes<MyAvatarRef>>;
export default MyAvatar;
